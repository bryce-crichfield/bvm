// package heap

// trait BlockSelectionStrategy:
//     def apply(blocks: List[Blocks]): Block

// object BestFit extends BlockSelectionStrategy:
//     override def apply(blocks: List[Blocks]): Block =
//         ???